<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="32dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

        <ImageView
            android:layout_width="@dimen/logo_laucher"
            android:layout_height="@dimen/logo_laucher"
            android:layout_gravity="center"
            android:layout_marginBottom="16dp"
            android:foregroundTint="@color/colorLight"
            android:scaleType="center"
            android:src="@drawable/ic_insert_drive_file_24dp" />

        <TextView
            android:id="@+id/info_menus_texto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Esta lei não possuí Artigos."
            android:textAlignment="center"
            android:textAppearance="@style/TextAppearance.AppCompat.Title"
            android:textColor="@color/colorLight" />
    </LinearLayout>

</FrameLayout>